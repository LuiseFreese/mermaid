# Frontend Local Development Configuration
# This file is for local development only and should NOT be committed to git

# Azure AD Configuration (Optional for Local Development)
# Leave these commented out if you're running with AUTH_ENABLED=false on the backend
# Uncomment and configure if you want to test authentication locally
# VITE_AZURE_AD_CLIENT_ID=your-azure-ad-app-registration-client-id
# VITE_AZURE_AD_TENANT_ID=your-azure-tenant-id
# VITE_AZURE_AD_REDIRECT_URI=http://localhost:3003

# API Configuration (automatically detected, but can override if needed)
# VITE_API_BASE_URL=http://localhost:3003
