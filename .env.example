# Mermaid to Dataverse Converter Configuration
# Copy this file to .env and update with your actual values

# Azure Configuration
# Find your Tenant ID in Azure Portal > Azure Active Directory > Properties
TENANT_ID=your-azure-tenant-id

# Find your Client ID in Azure Portal > App Registrations > Your App > Overview
CLIENT_ID=your-app-registration-client-id

# Find your Managed Identity Client ID in Azure Portal > Managed Identities > Your Identity
MANAGED_IDENTITY_CLIENT_ID=your-managed-identity-client-id

# Azure AD Authentication Configuration
# See docs/AZURE-AD-SETUP.md for detailed setup instructions
AZURE_AD_TENANT_ID=your-azure-tenant-id
AZURE_AD_CLIENT_ID=your-azure-ad-app-registration-client-id

# Authentication Control
# Set to 'false' for local development without authentication
# IMPORTANT: Must be 'true' in production environments
AUTH_ENABLED=true

# Dataverse Configuration
# Find your URL in Power Platform Admin Center > Environments > Your Environment
DATAVERSE_URL=https://your-dataverse.crm.dynamics.com

# Solution Configuration
SOLUTION_NAME=your-custom-solution-name

# Azure Resource Configuration (for testing and deployment)
RESOURCE_GROUP=your-azure-resource-group

# Optional: Authentication Configuration
# AUTH_MODE=managed-identity (default)
# PORT=8080 (default)

# Testing Configuration (for integration tests only)
# USE_MANAGED_IDENTITY=true
# USE_FEDERATED_CREDENTIAL=true
